<div class="posts list-group w-75">
  {{#each posts}}
    <a class="list-item flex-row justify-space-between btn btn-info border" href="/dashboard/edit/{{id}}">
      <span>{{title}}</span>
      <span>{{format_date createdAt}}</span>
    </a>
  {{/each}}
</div>